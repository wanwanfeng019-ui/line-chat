name: Keep Render Awake
on:
  schedule:
    - cron: '*/14 * * * *'  # every 14 minutes (free)
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS https://line-chat-z4m0.onrender.com/ > /dev/null
      - run: echo "Pinged at $(date)"
